# 備份用的 service_role 金鑰 — 絕對不可以進版控
backup/.service_key

# 備份輸出(預設在 OneDrive,這行只是保險)
steelcore-backup/

# 資料庫 migration SQL 只留在本機,不放上 GitHub
*.sql

# macOS
.DS_Store

# 含個資的試算表(薪資、出勤)一律不進版控
*.xlsx
*.xls

# Claude Code 本機專用設定
.claude/settings.local.json
